import { View, Text } from 'react-native'
import React from 'react'
import { ScrollView } from 'native-base'

const TV = () => {
  return (
    <ScrollView>
      
    </ScrollView>
  )
}

export default TV